# FundReady backend - environment template.
# Copy to `.env` and fill in real values. NEVER commit `.env` or any real secret.
# Every key this service reads must be listed here, with an empty value.

# ---------------------------------------------------------------------------
# Application
# ---------------------------------------------------------------------------
APP_ENV=development          # development | staging | production
LOG_LEVEL=INFO               # DEBUG | INFO | WARNING | ERROR
API_BASE_URL=https://fundready-5fkw.onrender.com
# Comma-separated; restrict to known origins. Next.js may bind 3001/3002 when 3000 is taken.
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,http://localhost:3002,http://127.0.0.1:3002,http://localhost:8097,http://127.0.0.1:8097,http://localhost:8081,http://127.0.0.1:8081,http://localhost:19006,http://127.0.0.1:19006

# ---------------------------------------------------------------------------
# Database (Neon: serverless Postgres + pgvector)
# ---------------------------------------------------------------------------
# Least-privilege application role. Migrations may use a separate, higher role.
# The plain postgresql:// form is fine; the async driver is applied for you.
DATABASE_URL="postgresql://neondb_owner:npg_I49hndjycgCf@ep-restless-tooth-avg6wmc9-pooler.c-11.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"
# ---------------------------------------------------------------------------
# Authentication (self-built -- AUTH.md)
# ---------------------------------------------------------------------------
# Signs our access tokens. At least 32 characters; a short secret makes every
# token forgeable offline. Generate with:
#   python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET_KEY="BwQnpI6vSguwd68R6Hubr27TCccgSs8SRI_VZK9trU7lzaTxUe1IJsEPKtGVJi7h"
JWT_ALGORITHM=HS256
JWT_KEY_ID=k1              # bump when rotating the signing key
JWT_ISSUER=fundready
JWT_AUDIENCE=fundready-api
ACCESS_TOKEN_TTL_MINUTES=15
REFRESH_TOKEN_TTL_DAYS=30

# Argon2id cost. Memory cost is real RAM per concurrent login -- on a small
# instance, lower it rather than running out. Production floors: 19456 / 2.
ARGON2_MEMORY_COST_KIB=65536
ARGON2_TIME_COST=3
ARGON2_PARALLELISM=1

# Encrypts TOTP secrets at rest, so a database leak does not defeat MFA.
MFA_SECRET_ENCRYPTION_KEY=tUWqfMqB7Rva1zLXz1Ap6norQ6at4QUE-5mYorTG9FlP_de5-77HGrW_EPNHYwM_

# ---------------------------------------------------------------------------
# Object storage (Cloudflare R2 -- documents + evidence, never in Postgres)
# ---------------------------------------------------------------------------
R2_ACCOUNT_ID="ec8648991366eda522acdd94940b1d02"
# https://<account-id>.r2.cloudflarestorage.com
R2_ENDPOINT_URL="https://ec8648991366eda522acdd94940b1d02.r2.cloudflarestorage.com"
R2_ACCESS_KEY_ID="b57e6a891ae0b307892c8912430d2271"
R2_SECRET_ACCESS_KEY="5f1fd9a34b54a77ecefd7b95056d1759d328e5c8fdf6664496f63dc42f201d25"
R2_BUCKET_DOCUMENTS=documents
R2_BUCKET_EVIDENCE=evidence
STORAGE_SIGNED_URL_TTL_SECONDS=900

# ---------------------------------------------------------------------------
# Queue (Redis)
# ---------------------------------------------------------------------------
REDIS_URL="rediss://default:gQAAAAAAAwJOAAIgcDFjOGNmNDNkMzY5N2U0NDI5YWQxOWYxOGFmZjk4NDNmMg@quality-snail-197198.upstash.io:6379"
# The two REST vars below are read by nothing in this codebase. RQ speaks the
# Redis wire protocol; they are kept only because Upstash shows them first.
QUEUE_NAME=fundready

# ---------------------------------------------------------------------------
# AI (Claude API) - model tiering and budgets per DECISIONS.md D16
# ---------------------------------------------------------------------------
ANTHROPIC_API_KEY="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"
# Strongest model: audits, scoring, evidence. Blank uses `claude-opus-5`.
AI_MODEL_AUDIT="claude-opus-5"
# Cheaper model: founder + investor chat. Blank uses `claude-sonnet-5`.
AI_MODEL_CHAT="claude-sonnet-5"
AI_MAX_OUTPUT_TOKENS="16000"
AI_DAILY_BUDGET_TOKENS_PER_USER="20000"

# ---------------------------------------------------------------------------
# Stripe (billed through the UK/US entity; no Connect in v1 - DECISIONS.md D5)
# ---------------------------------------------------------------------------
STRIPE_SECRET_KEY=
# Required; webhook signatures are always verified.
STRIPE_WEBHOOK_SECRET=
STRIPE_PUBLISHABLE_KEY=

# ---------------------------------------------------------------------------
# Email (transactional)
# ---------------------------------------------------------------------------
RESEND_API_KEY="re_Ydh9F4ks_5oXYUm45g6aJvxwf3CbWEnZB"
EMAIL_FROM_ADDRESS=FundReady <noreply@stephenakintayofoundation.org>

# ---------------------------------------------------------------------------
# Monitoring
# ---------------------------------------------------------------------------
SENTRY_DSN=
APP_LINK_BASE_URL="https://stephenakintayofoundation.org/app"

# Neon's DIRECT endpoint (no `-pooler`): alembic's DDL target.
DATABASE_MIGRATION_URL="postgresql://neondb_owner:npg_I49hndjycgCf@ep-restless-tooth-avg6wmc9.c-11.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require"

MFA_SECRET_ENCRYPTION_KEY="AuE5KBVItkj1Gg5jh5PACOhZd1tORsQPqkeDt7Ju9u4"

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:3001,http://127.0.0.1:3001,http://localhost:3002,http://127.0.0.1:3002,http://localhost:8097,http://127.0.0.1:8097,http://localhost:8081,http://127.0.0.1:8081,http://localhost:19006,http://127.0.0.1:19006
APP_LINK_BASE_URL=http://localhost:8097