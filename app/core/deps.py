"""Shared FastAPI dependencies.

Composes the pieces from `config`, `db`, and `security` into the dependencies
routers declare (database session, current user, role and condition gates).

Implemented in TASKS.md T0.3 / T0.4.
"""
