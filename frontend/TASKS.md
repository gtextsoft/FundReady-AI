# TASKS.md — FundReady Mobile Frontend

The build queue for the Expo app. Same rules as `backend/TASKS.md`: **do one task at a time**, top to bottom, finish it (code + verification + docs), check it off, then move on. Acceptance notes are the bar for "done."

Legend: `[ ]` todo · `[x]` done · `[~]` in progress

**Phases are numbered to line up with the backend's.** An `F`-task usually cannot close before the `T`-task it names, because the client cannot show data no endpoint returns. Where the backend is already ahead of the client, that is called out — those are the cheapest tasks in this file.

**What "done" means here.** A green `npx tsc --noEmit` proves almost nothing about this app: every trap in `AGENTS.md` §"Traps that do not fail the build" compiles and typechecks while rendering wrong. A task closes when the screen was **driven in a browser or on a device**, not when it built.

> **Standing caveat on the 2026-08-01 batch (F0.1, F0.2, F1.5–F1.8).** Those closed against `npm run lint`, `npm run typecheck`, 88 Jest tests and a clean `expo export --platform web` — which proves Metro resolves every import and that `/reset-password` is a real route. **None of those screens has been rendered in a browser or pointed at a running backend.** The transport is covered by tests at the `fetch` boundary, so the request shapes are checked against what the API's routers declare; what is *not* checked is the API actually answering them, and nothing about how any of it looks. Run the app against a local backend before trusting the ticks — the rendering half is F0.5's job and F0.5 is still open.

---

## Phase F0 — App setup

- [x] **F0.1 Scaffold** — Expo SDK 57 / React Native 0.86 / expo-router / NativeWind 4 under `src/`, `@/*` path alias, `.env.example`, developer documentation. *Done when: the layout exists, `npx tsc --noEmit` is clean, and the README tells a new developer how to run the app against the backend.* — closed 2026-08-01. The structure, the alias and `.env.example` (with the correct warning that `EXPO_PUBLIC_*` ships to the device readable) were already there; `README.md` was still the stock `create-expo-app` template, advertising `npm run reset-project` — which moves this entire app aside — and has been replaced with run/env/check instructions that point at `AGENTS.md` for the traps.
- [x] **F0.2 Tooling & CI** — lint, format, type-check and a test runner, all run on every push. *Done when: CI is red on a type error and on a lint error, in a job that actually looks at `frontend/`.* — closed 2026-08-01. Before this, `package.json` had a `lint` script (`expo lint`) with **no ESLint installed and no config**, zero test files, and a root workflow that ran `ruff`/`mypy`/`pytest` only: **no CI job had ever looked at this directory.** Now `eslint.config.js` (flat, on `eslint-config-expo`), `jest.config.js`, 88 tests in `test/`, and a `frontend` job in `/.github/workflows/ci.yml` running `npm ci` → lint → typecheck → test with `working-directory: frontend`. Four things worth knowing:
  - **ESLint found 11 errors on its first ever run**, which is the whole argument for the task. Nine were fixed (three unused imports; `useMemo(upcomingDays, [])` never memoising because the argument was not an inline function; `useRef(new Animated.Value(…)).current` read during render in `score-ring.tsx`, now a lazy `useState`). Two were left with a targeted disable and a written reason: the `PanResponder` handlers in `slider.tsx` read a ref, but only after a gesture starts, and the `setLimit` reset in `dealflow-screen.tsx` — the idiomatic fix there is to key the component on the filter, which changes how parents mount it, and there is no rendering test to catch what that breaks.
  - **`jest-expo` is deliberately not a dependency.** Its peer range wants `@react-native/jest-preset@^0.86.2` while `react-native@0.86.0` pins that package to exactly `0.86.0`, so installing it needs `--legacy-peer-deps` — and then `npm ci` in CI needs it too. A CI that has to be told to ignore a broken dependency graph is not much of a gate. Native modules are mapped to stubs in `test/stubs/` instead, including `expo/virtual/env` (which `babel-preset-expo` injects as untransformed ESM).
  - **The cost of that: there are no component rendering tests.** Every trap in `AGENTS.md` — dropped `className`, two colour utilities on one element, nested `Pressable` — still passes `npm test` while rendering wrong. Jest covers the transport, the gates and the parsing; it does not cover the pixels. That is F0.5's job.
  - **Prettier is installed but is not a gate.** The app predates it by ~80 files at any print width, so `--check` fails everywhere and a repo-wide reformat would bury real changes in whitespace. The CI step is `continue-on-error`. Run `npm run format:write` to take that diff in one commit, then promote it.
  - No `paths:` filter, deliberately: a frontend-only push still runs the backend suite and vice versa. It is slower, but a filtered job that never runs reports success it did not earn.
- [x] **F0.3 Design system** — `theme/tokens.ts` mirroring `tailwind.config.js`, Archivo + JetBrains Mono, the `components/ui/` primitives, `css-interop.ts` registration for non-core components. *Done when: a screen can be built without inventing a colour or a font weight.* — the token/Tailwind duplication is deliberate and documented: Tailwind cannot reach SVG strokes, gradient stops or data-driven colours, so `C` and `band()` exist for those. Two invariants hold and must keep holding: `Txt*` drops its default colour when the caller supplies one (two colour utilities on one element resolve by stylesheet order, not class order — bone-on-bone), and weight comes from the font family (`font-med`/`font-semi`/`font-bold`), never `fontWeight`, because Archivo ships one file per weight. **Re-skinned to FundReady brand v1.0** — the palette went from the black/white/blue prototype to obsidian + rationed signal green (see `AGENTS.md` §Brand); Geist was replaced by Archivo and JetBrains Mono. The colour *names* changed with it (`ground`→`obsidian`, `ink*`→`bone*`, `surface-*`→`carbon-*`, `line-*`→`graphite-*`, `grn`/`amb`/`red`→`signal`/`flag`/`alert`), so any patch written against the old names will silently render unstyled rather than fail to build.
- [x] **F0.4 Navigation & role separation** — `founder/` and `investor/` as **real path segments, not route groups**, each with its own `(tabs)`; `role-guard.tsx`; `index.tsx` as the entry redirect; typed routes on. *Done when: an investor cannot reach a founder route by typing its URL, and the two sides cannot collide on a route name.* — the segment choice is what makes the guard possible: a group would let both sides claim `/profile` and leave the guard with nowhere to bounce to. `lib/routes.ts` casts hrefs once because typed routes emit `/founder/index` while the runtime wants `/founder`.
- [~] **F0.5 UI verification harness** — `scripts/verify-ui.js`, driving both sides end to end through Playwright against installed Edge, asserting on computed styles and rendered text. *Done when: the harness passes against the current app and catches the `AGENTS.md` traps.* — **built (479 lines) but now stale, and it will fail if run today.** It was written against `src/api/mock.ts`, which was deleted in `3925df7`; four of its steps still assume mock behaviour that no longer exists ("the mock approves on a timer", "the mock answers from the founder's own numbers — 1450/320 = 4.5:1", and the two comments about resetting mock module state). Everything past sign-in now hits `not_implemented`. **Decide before repairing it:** either it re-targets a live local backend (real value, needs the API running and a seeded account), or it narrows to auth + the render traps + the `Unavailable` states and stops claiming to cover the product. The second is honest and cheap; the first is what actually protects the release.

- [ ] **F0.6 Retire the `sacifundme://` scheme** — the FundReady rebrand left the URL scheme behind on purpose. It is half of a contract with the backend: the server builds `APP_LINK_BASE_URL` from it, and `https://` links do not open the app yet (no `associatedDomains`, no `intentFilters`), so the scheme is the only thing that reaches a phone. *Done when: `app.json` registers `fundready://`, the backend's `APP_LINK_BASE_URL` is changed in the same release, and a verification link built by the server opens the app.* Two things to decide first: whether to keep `sacifundme://` registered alongside as a second scheme so links already sitting in inboxes still resolve, and whether to do this at the same time as configuring Universal/App Links, which would remove the need for a custom scheme in the first place. Both `verify-email` and `reset-password` keep their paste-the-code field either way.

## Phase F1 — Backend seam & authentication

- [x] **F1.1 API seam** — `api/contract.ts` as the one interface the app knows, `api/index.ts` choosing the implementation. *Done when: no screen imports `fetch`, and swapping the implementation touches no screen.* — the mock and its 24-company seed dataset were **deliberately deleted** (`3925df7`), not left switchable. The reason is worth keeping: a mock that renders plausible numbers is indistinguishable, on screen, from a working feature, and this app is far enough ahead of the backend that the difference matters. Everything unbuilt now throws `not_implemented` and `components/unavailable.tsx` renders it as a dashed **NOT BUILT YET** panel — visually distinct from the red **COULD NOT LOAD** it shows for a real failure.
- [x] **F1.2 HTTP transport** — `api/http.ts`: base-URL resolution, the backend's error envelope mapped to `ApiFailure` codes, 401 → refresh → retry, token pair at rest. *Done when: a 401 transparently refreshes once, and a replayed refresh cannot be triggered by two screens loading at once.* — three calls worth knowing. **The refresh is single-flight** (`refreshing ??=`): the server treats a second presentation of a rotated refresh token as theft and revokes the whole family, so two concurrent 401s starting two exchanges would log the user out — the client's concurrency is a security property here, not a performance one. **The new pair is stored before it is returned**, because the old one is already spent and losing the new one locks the account out until a fresh login. **The base URL is derived from whatever served the bundle** (`8f59f56`) rather than baked in: a phone cannot reach `localhost`, and DHCP moves the LAN address, so `EXPO_PUBLIC_API_URL` wins when set and the dev-server host is inferred otherwise — with a separate `globalThis.location` branch for web, where `hostUri` is never populated.
- [x] **F1.3 Sign-up, sign-in, sign-out** — `sign-up.tsx`, `sign-in.tsx`, the founder company-domain rule, names collected and sent. *Done when: a founder on a consumer mailbox is refused with a reason, and a registered account can sign in and out against the live API.* — **no consumer social login; Google and LinkedIn were removed deliberately.** `domain/email.ts` works by blocklist rather than by trying to prove a domain is corporate: a false reject turns a real founder away, which costs more than letting an unusual domain through, and the company is verified properly later. The client check is convenience — the server is the control, and as of backend T1.4a it enforces the same rule. **Registration sends `first_name`/`last_name` again as of `3c1fcc3`**; the 422 that broke every mobile registration between 2026-07-30 and that commit is closed (`backend/TASKS.md` still carries it as open — see the follow-ups below). Registration answers 202 with no tokens and answers identically whether or not the address was taken, so `signUp` logs in immediately afterwards: that round trip is what proves an account exists.
- [x] **F1.4 Two-factor login** — the `mfa_required` branch, `MfaRequired`, `mfa.tsx`. *Done when: a login that needs a second factor lands on the code screen and the exchanged pair is a real session.* — the shape matters: login returns `status`, and **`tokens` is null on the MFA path** while a short-lived `mfa_token` carries the challenge. Branching on `status` rather than assuming `tokens` is what stops a null dereference becoming a silent signed-out state. Admins are refused a session in the app entirely (`sessionFrom` throws `forbidden` and clears the tokens) — the console is the admin surface, per backend `AUTH.md` §9.
- [x] **F1.5 Email verification** — `verify-email.tsx`. *Done when: a founder can confirm their address from the emailed link without typing anything, and the screen notices the moment the server marks it confirmed.* — closed 2026-08-01 by F1.7. The state half was already real: `emailVerified` is read off `/v1/users/me` on every load and on focus, never cached, so the screen agrees with the server the instant it changes. What was missing was the link — the screen asked the user to *paste a token into a text field*. It now reads `?token=`, submits once on arrival behind a ref guard (these are single-use; a re-render spending the token again would fail the second attempt), hides the paste field when the link supplied one, and keeps it when it did not. **The screen now works signed out**, which it did not before: whoever follows the link may not be signed in on that device, the endpoint takes no authorization, and a `confirmedHere` flag renders the result when there is no account to re-read. Two things still open, both backend-side: `resendVerificationEmail` throws `not_implemented` because no resend endpoint exists, and `https://` links do not open the app until F5.5 configures Universal/App Links.
- [x] **F1.6 Password reset** — `forgot-password.tsx` + `reset-password.tsx`. *Done when: a user can complete a reset end to end, from the email to a new password to a working login.* — closed 2026-08-01. The missing half was not on the contract at all: nothing took a token plus a new password, so a founder who requested a reset reached a dead end while a working backend endpoint sat unused since 2026-07-30. Added `resetPassword(token, password)` to `FundReadyApi`, `/v1/auth/password-reset/confirm` in the transport, and the screen. Design calls:
  - **The local token pair is dropped on success.** The server has just revoked every refresh token and invalidated every access token it issued, so what the device holds is dead — and if a *different* person completed the reset, keeping it would leave a live-looking session on a device they now control. A *failed* reset changes nothing server-side, so the session is deliberately left alone; both are asserted.
  - **"Signed out everywhere" is stated, not implied.** Otherwise the other device dropping to the sign-in screen reads as a fault rather than as the point.
  - The password rule moved out of `sign-up.tsx` into `domain/password.ts`, shared by both screens. Two screens now mint passwords, and a client that lets through something the server refuses turns a typo into an unexplained `422`. No composition rule, deliberately — length is the defence.
  - Fixed while here: `forgot-password.tsx` told the user the link expires in 30 minutes. The backend's `PASSWORD_RESET_TTL` is an hour.
- [x] **F1.7 Deep links** — consume the `APP_LINK_BASE_URL` links: verification token, reset token, signed-out fallback. *Done when: tapping the link in a real email opens the app on the right screen with the token already applied, and the same link opened by a signed-out user still works.* — closed 2026-08-01. **There is deliberately no URL parser.** expo-router's own linking already resolves `sacifundme://reset-password?token=x`, the Expo Go `exp://…/--/…` form and the plain web URL onto the same route with the same search params; a hand-rolled parser would be a second, worse implementation of something already in the bundle. What was left is reading one parameter safely, which is `lib/deep-link.ts`: array-valued repeats, the trailing newline a wrapped URL picks up, and `?token=` (empty) returning null rather than posting nothing and earning a generic "this link is invalid". **The two route segments are now a contract with the server** — `verify-email` and `reset-password` — and renaming either breaks every link already in an inbox; `lib/routes.ts` says so. **Still open: `https://` links do not open the app.** Universal Links (`associatedDomains`) and App Links (`intentFilters`) are not configured, so `APP_LINK_BASE_URL` must be the custom scheme to reach a phone. That is F5.5, and it is why both screens keep a paste-the-code fallback.
- [x] **F1.8 Startup profile** — wire `getProfile`/profile editing to the real `T1.4` endpoints. *Done when: a founder's profile round-trips to the server and survives reinstalling the app.* — closed 2026-08-01. `getProfile`, `saveProfile` (create-then-update, with the 409 race handled: two devices both read "none yet", both `POST`, and the loser must update the profile that now exists rather than report a failure), and `getFounderAccount` now preferring the **stored** company name over the guess from the email domain. Onboarding reads the saved profile back on mount, so four screens of typing survive being abandoned halfway. **The important finding is the shape mismatch**, and it needs a product decision:
  - **Six of the form's fifteen answers have nowhere to go on the server.** `growth`, `tam`, `margin`, `ltv` and `technical` have no field in `modules/intake/fields.py` at all; `Bootstrapped` is not one of the server's stages. The form came from the design prototype and the field list was written independently.
  - **They are reported, never swallowed.** `toWireProfile` returns them in `unmapped` with a reason each, and the results screen renders a **NOT SAVED YET** panel listing them. Dropping them silently is the worse failure: the founder types a number, the screen accepts it, and later the audit is silent about it with nothing anywhere explaining why.
  - **Nothing is derived.** Revenue × (1 − margin) *would* yield `cost_of_revenue_minor`, and it is tempting, but a figure the client computed and labelled `source: founder` is a fabricated input to an audit that has to cite its evidence (`CLAUDE.md` §5). If those derivations are wanted they belong on the server, marked as derived. A test asserts the client does not do it.
  - **Money needs a currency, and the currency comes from the country.** The form asks for free text ("Lagos, Nigeria") and the server wants ISO 3166-1 alpha-2 plus ISO 4217. An unrecognised location is reported unmapped rather than guessed — a wrong country silently selects the wrong benchmark set and the founder is scored against the wrong market with no sign of it — and money is withheld entirely rather than stored as an integer whose unit nobody knows. Zero-decimal currencies are handled: multiplying yen by 100 would overstate revenue 100-fold.
  - Expect this file to move: `fields.py` is flagged **provisional** in the backend's own task list pending the missing spec.
- [ ] **F1.9 Document upload** — deck and financials to R2 through the backend's signed URLs, with progress and failure states. *Done when: a real file uploads from a device and comes back down through an expiring URL.* — the onboarding step already picks a file with `expo-document-picker` and holds a filename. Blocked on backend T1.5, which is built but has **never written a byte** (no R2 buckets, no keys). Client-side notes for when it unblocks: the presigned PUT cannot enforce size (the server checks afterwards with `head_object` and deletes anything oversized), so the **client must enforce the limit before uploading** or the user watches a long upload get silently discarded; and the declared content type is pinned into the signature, so it has to match the bytes exactly.
- [ ] **F1.10 Company verification** — `founder/verify.tsx` against a real endpoint. *Done when: a submitted registration document moves the account to `in_review` server-side and the screen reflects it.* — screen is built (235 lines, including the upload); `submitCompanyRegistration` throws `notYet('T1.4')`. **No backend task covers company-registration verification** — T1.4a supplies the company *name* from the domain, which is not the same thing, and `kyc_status` is investor identity (the client is careful not to confuse the two). Raise it as backend scope before building further here.

## Phase F2 — Audit surface

> **The backend is deployed** at `https://fundready-ai.onrender.com` (`/docs`, `environment: staging`), and it is a long way ahead of where this file assumed it was. Audits, tasks, evidence, benchmarks, discovery and interests all have endpoints now — 46 of them. `.env` points at it.
>
> **Verified live on 2026-08-06**, against the deployed service, on a real account: register → login → `/users/me` → verify-email → create profile → queue an audit. Every wire shape in `api/http.ts` matched what came back. A replayed verification token was correctly refused with `422`, and `/v1/startups/me` returned `404 "You do not have a startup profile yet"` before the profile existed — which is the case the client maps to `null`.
>
> **Three things to know, all server-side:**
>
> 1. **The audit worker is not running, so audits queue forever.** `POST /v1/startups/{id}/audits` returns `202` and the run sits at `queued` with `attempts: 0` indefinitely — polled for four minutes, `started_at` never set. Confirmed against Redis directly: **2 jobs waiting, 0 workers registered.** `render.yaml` declares two services — `fundready-api` and `fundready-worker` (`python -m app.workers.queue`) — and its own header warns that "a deploy without the worker accepts submissions that nothing will ever pick up." That is exactly the state. Deploying the worker is what closes F2.2 and F2.3; nothing on this side is waiting.
> 2. **No browser origin is allowed — and that is deliberate, not a misconfiguration.** `render.yaml` sets `CORS_ALLOWED_ORIGINS: ""` with the note "Empty is correct for a mobile-only client". So the deployed service answers every preflight with `400` and no `access-control-allow-origin` (checked against `localhost:8097`, `:8081`, `:19006`, `:3000` and its own origin). **The consequence is real regardless of intent: the web preview cannot reach staging**, so `scripts/verify-ui.js` and browser testing need either a local backend or that origin added. Expo Go and native builds are unaffected, CORS being a browser rule rather than an HTTP one.
> 3. **Every route past registration requires a confirmed email address.** `/v1/startups/me`, `/v1/discover`, `/v1/interests` and every audit route answer `403 "Verify your email address to continue."` until then. That is the normal state of everyone arriving for the first time, so it is handled as an instruction rather than an error: `results.tsx` reads `emailVerified` off the account and offers the verification screen. It is read off the account rather than pattern-matched out of the message, because the server sends a plain `forbidden` with no distinct code and matching on prose would break the first time it is reworded.
>
> **Email delivery works** (checked against the Resend account, 2026-08-06): the key is valid, `stephenakintayofoundation.org` is verified, and 17 of 42 messages have been delivered to real mailboxes. The rest went to unroutable test domains. Emailed links are `https://stephenakintayofoundation.org/app/verify-email?token=…`, which is the format `lib/deep-link.ts` and F1.7 were built against — confirmed by minting one with `backend/scripts/issue_dev_token.py`. That script is also the way past the email gate when testing, without waiting on a mailbox.

- [~] **F2.1 Assessment intake** — the four-step onboarding form, per-step validation, resumable. *Done when: a complete profile is submitted to the audit engine and the app stops scoring anything itself.* — **the form is done** (`(onboarding)/onboarding.tsx`, `isStepValid` gating each step) and, since F1.8, it is **resumable and persisted**: `load()` reads the stored profile back on mount, and `submit()` saves before scoring so answers survive even though the audit engine does not exist. What is not done is the submission: `submitAssessment` throws `not_implemented`, so `submit()` resolves `null` by design and the results screen falls back to `liveAssessment()` — a heuristic over the form computed on the device, **explicitly labelled provisional on screen**. That labelling is the only thing keeping this honest, and it is exactly what must be deleted when T2.7/T2.8 land: two scoring models, one of which is not the audit, is the false-verdict failure `CLAUDE.md` §5 exists to prevent. Treat `domain/scoring.ts` as scaffolding with a demolition date.
- [~] **F2.2 Audit run status** — submit, then poll the status endpoint; a real pending state that survives backgrounding the app. *Done when: a submitted audit shows genuine progress and recovers if the app is closed mid-run.* — **built, not yet verified against a real run.** `requestAudit` / `getAuditRun` / `listAuditRuns` are wired; the assessment screen queues the audit and the results screen polls every 3s to a terminal state. `resumeAudit()` picks the newest run back up on arrival, because an audit takes minutes and outlives the screen that started it. The poll gives up after five minutes — which stops the *polling*, not the audit, so the cost is a wait rather than a result. Polling also stops if a newer run is started, so an abandoned run cannot overwrite the one being watched. **What is unproven: no audit has ever actually run.** Closing this needs a confirmed account (see the CORS/email note above).
- [~] **F2.3 Report & action plan** — verdicts, findings and the action plan, rendered from the founder tier. *Done when: every conclusion on screen shows what it was based on, and `insufficient_data` reads as "we could not tell", never as a low score.* — **built, never seen with real data.** `components/founder/audit-report.tsx` renders both verdicts, the findings, and the action plan with priorities first. The rule it exists to keep: **a null score is not a zero.** The server returns `score: null` when the audit declined to reach a verdict, and the mapper passes it through un-defaulted — a `?? 0` anywhere in that path would put a scored zero on screen for a conclusion the audit explicitly refused to draw. A test asserts it. `unevidencedDimensions` is rendered as part of the verdict rather than as a footnote, for the same reason.
  - **The prototype's score is now superseded, not supplemented.** `submitAssessment` (0–100, weighted parts, VC/PE fit, colour band) has no server equivalent and stays unimplemented rather than being faked out of a `FounderReport`. The on-device estimate still renders **only while there is no real report**, still labelled provisional; the moment one arrives it replaces it. Two scores on one screen, one of which is not the audit, is the exact ambiguity the label exists to prevent. `domain/scoring.ts` can go once F2.2 is confirmed working.
- [ ] **F2.4 Missing-data UX** — surface `missingFields` and per-field confidence from the profile as the thing to go and fix. *Done when: a founder can see exactly which gap is holding their score down and act on it.*

## Phase F3 — Readiness loop, billing & mentor

- [ ] **F3.1 Task list & evidence** — required vs recommended tasks from `T3.1`, evidence upload against `T3.5`, pass/fail/needs-more with reasons. *Done when: failing evidence shows why it failed, in words the founder can act on.*
- [~] **F3.2 Programmes & events** — `founder/programmes.tsx`. *Done when: the catalogue is server-driven, filtered by region, and enrolment persists.* — screen built; `enrol` throws `notYet('T3.2')`. It currently renders a fixed pair of programmes (`readiness` | `wealth`) while backend D18 models one catalogue with a `program · mentorship · event` discriminator — so the screen's shape will have to give when the real data arrives.
- [~] **F3.3 Payments** — `founder/paywall.tsx` and the one-off unlock. *Done when: a real Stripe checkout completes on a device and the unlock is read back from the server, never from the client.* — screen built (132 lines) and the trial/payment gate in `domain/access.ts` is real; `purchaseUnlock` throws `notYet('T3.3 / T3.4')`. **The client must never decide it has been paid** — backend T3.4 puts subscription state on Stripe's word alone, and `getFounderAccount` already reads `subscription_status` off `/v1/users/me`, which is the right seam. Checkout is hosted; this app should never see card data.
- [ ] **F3.4 Trial expiry** — the 14-day window enforced against server time. *Done when: changing the device clock does not extend the trial.* — `TRIAL_DAYS` is currently applied to `created_at` **on the device**, which means the trial boundary is computed from a clock the user controls. Fine while nothing is being withheld; not fine once F3.3 is real.
- [~] **F3.5 Founder AI mentor** — `founder/ai-mentor.tsx`. *Done when: answers come from `T3.7`, streamed, scoped to the founder's own audit.* — screen built (135 lines); `askMentor` throws `notYet('T3.7')`. Two client-side requirements when it lands: the response streams (a 16k-token answer behind a spinner reads as a hang), and anything the model says about the founder's own numbers is traceable back to the audit that produced them.

## Phase F4 — Investor side

- [~] **F4.1 Investor verification** — `investor/verify.tsx` against Stripe Identity (`T4.1`). *Done when: an un-verified investor cannot request an introduction or schedule a call, and the flow completes on a device.* — screen built; `submitInvestorCredentials` throws `notYet('T4.1')`. The status mapping is already correct (`kyc_status` → `VerificationStatus`, one-to-one), and the gate is enforced in `domain/access.ts` rather than by hiding buttons — locked features are shown and explained, which is the right call and should survive the wiring.
- [~] **F4.2 Dealflow** — `components/investor/dealflow-screen.tsx`, filters, sort, pagination. *Done when: the list is server-ranked and contains nothing above summary tier.* — **the API half is wired; the screens are not, and this is now a design task before it is a coding one.** `discoverStartups` / `getDiscoveredStartup` / `expressInterest` call `/v1/discover` with faithful types in `domain/discovery.ts`. What blocks the screens is that **the summary tier is far thinner than the prototype's card assumes**: it carries the name, sector, stage, country and the two verdicts — and *no* MRR, growth, margin, LTV/CAC, runway, team, memo or risk flags. Every one of those is full-report material that reaches an investor only after an admin reveal (F4.6). So `listCompanies`/`getCompany` deliberately still throw: building a `Company` out of a `StartupCard` would mean inventing every number the card draws. A test pins that refusal. **The question to answer first is what a dealflow card shows when there is no MRR to show.**
  - The server's filters are `sector`, `stage`, `country`, `limit`, `offset`. `DealflowQuery` also carries free text, a minimum score, a VC/PE match type and four sort orders — none of which `/v1/discover` accepts. Filtering the difference client-side would mean fetching rows the investor was not meant to receive in order to hide them again, which defeats the tier.
  - The interest `note` is **context for SACI and the founder never sees it.** Say so in the UI: an investor writes differently when the subject is not reading.
- [~] **F4.3 Company deep dive** — `investor/company/[id].tsx`. *Done when: the summary tier renders and no field outside it is ever requested.* — same blocker and same seam as F4.2; `getDiscoveredStartup` is wired, the screen still expects `Company`.
- [ ] **F4.4 Watchlist** — persist across devices. *Done when: a watchlist survives a reinstall.* — **no backend task exists for this.** `getWatchlist`/`toggleWatch` are on the contract and both throw with the note "no backend task yet". Either it gets one, or the client owns it as local-only state and the contract stops implying otherwise.
- [~] **F4.5 Introductions & scheduling** — `schedule-call-sheet.tsx`, the request/accept loop, SACI in the middle. *Done when: an interest moves through its states in the app with SACI as a required participant (`T4.5`).* — sheet built (142 lines) and the notification loop is modelled in `store/notifications.ts`; `requestIntroduction`, `requestCall`, `listCallRequests` and `respondToCall` all throw. Note the shape change coming: the client currently models a two-party request/accept, while T4.5 puts **admin approval in the middle** — there is a state in the backend's flow that has no screen here yet.
- [ ] **F4.6 Investor AI chat** — analyst chat restricted to summary-tier retrieval (`T4.4`). *Done when: the chat cannot surface data above summary tier even when prompted to.* — **no screen exists yet**; the investor tabs are dealflow, watchlist, alerts, profile. The restriction is enforced in retrieval server-side, not in the prompt and not here — the client's job is to not build an affordance that implies otherwise.

## Phase F5 — Hardening & release

- [~] **F5.1 Notifications** — `components/notification-centre.tsx` + both alerts tabs. *Done when: alerts arrive from the server and unread state is shared across devices.* — UI and store built; `listNotifications`/`markNotificationsRead` throw `notYet('T5.3')`. **Push notifications are not in scope anywhere yet** — no `expo-notifications`, no device-token registration, and no backend task that would receive one. That is a product decision to make, not an oversight to fix quietly.
- [ ] **F5.2 Offline, retry & error states** — a real story for a dropped connection and a 429. *Done when: killing the network mid-flow produces a recoverable state, not a spinner.* — `ApiFailure` already carries `network` and `rate_limited` distinctly, so the information is there; nothing consumes it yet beyond the generic panel.
- [ ] **F5.3 Accessibility pass** — labels, roles, touch targets, dynamic type, contrast. *Done when: both sides are navigable by screen reader and legible at the largest system font size.* — the design is dark, small-type and letter-spaced; this will find real problems.
- [ ] **F5.4 Analytics & crash reporting** — *Done when: a JS crash on a device produces a symbolicated report.* — pick the tool with the PRD's success metrics (backend T5.4) in hand, so the client and server measure the same funnel.
- [ ] **F5.5 Release builds** — EAS build/submit profiles, app icons and splash (already present), store listings, versioning. *Done when: a signed build installs from TestFlight and the Play internal track.*
- [ ] **F5.6 Environment config** — staging and production `EXPO_PUBLIC_API_URL`, per-channel builds. *Done when: a build points at the deployed API (backend T5.7) with no local edit.* — the rule in `.env.example` is the one to keep enforcing: **`EXPO_PUBLIC_*` is inlined into the bundle and readable by anyone with the app**, so nothing secret can ever go there. There is no client secret to hold, and it should stay that way.

---

### Open follow-ups

- [x] **`AGENTS.md` documented a mock that no longer exists** — fixed 2026-08-01. §"Where things live" described `src/api/mock.ts`, "the 24-company seed dataset" and `roleForSignIn`; the §"Verifying UI changes" notes described resetting mock module state. All of it went with `3925df7`. The file is the best documentation in this repo, which made the stale parts more dangerous, not less — they read as current. Also added: a §"Links from emails" section and a §"Checks" section, and the harness's staleness is now stated where the harness is described.
- [ ] **`backend/TASKS.md` carries a resolved item.** Its follow-up "Tell the mobile developer that registration is broken" was fixed here in `3c1fcc3`: `signUp` sends `first_name`/`last_name` and the 422 is gone. Tick it off there. *(Not edited from this side — it is the backend's file.)*
- [x] **Two stale doc comments in `src/`** — fixed 2026-08-01. `verify-email.tsx` said its actions were unbuilt (T1.2b closed 2026-07-30), and `http.ts` `signUp` said "the server does not yet enforce the company-domain rule for founders" (T1.4a closed 2026-07-31 and it does). The `http.ts` header claimed only authentication existed server-side; it now names what is live.
- [x] **`README.md` was the `create-expo-app` template** — replaced 2026-08-01. It advertised `npm run reset-project`, which moves the app aside.
- [ ] **Take the Prettier diff.** `npm run format:write` reformats ~80 files. Do it in one commit that changes nothing else, then make the CI step a gate instead of `continue-on-error`.
- [ ] **Six onboarding answers have no server field** (`growth`, `tam`, `margin`, `ltv`, `technical`, and the `Bootstrapped` stage). The app now tells the founder they are not being saved, which is honest but not a fix. Either `fields.py` grows them or the form stops asking — a product call, not a client one. See F1.8. **Proposal written: `docs/INTAKE.md`.** Tracing the form against `rubric/v1` changes the diagnosis: three of the six (`margin`, `ltv`, `tam`) are not missing fields but *wrong questions* — they ask for figures `finance.py` computes, and the rubric explicitly marks down an unsourced headline TAM, so the form is manufacturing its own low scores. The larger finding is the other direction: **eleven fields are absent**, and the three saleability dimensions (`owner_independence`, `transferability`, `revenue_durability`) have almost nothing feeding them, along with `scalability`'s central criterion — nothing anywhere holds *what the capital is for*.
- [x] **Drop the mock and talk to the real API** — done 2026-07-30 (`3925df7`, `b3d8a74`), with the API host derived from the dev server in `8f59f56` so a physical phone reaches the machine without a rebuild.
- [x] **Confirm `frontend/.env` is not committed** — checked 2026-07-31: ignored by root `.gitignore:10` and untracked. The root CI's `gitleaks` job scans this directory too, so a future slip would be caught.

### Backlog / open questions (resolve before the dependent task)

- [ ] **Paid detailed report, priced per country** — proposal in `docs/PAID-REPORT.md`. Charging the local equivalent of $17 for the detailed audit and its step-by-step action plan. **Decide the store question first: Apple and Google require in-app purchase for a digital good unlocked inside the app, take 15–30%, and price by their own tiers — so "the local equivalent of $17" is not something IAP can express, and a Stripe/Paystack card sheet inside the iOS app fails review.** Selling it on the web and having the app read the entitlement is the usual way out. Beyond that: the payer's country is not the company's country (no country field exists on the user at all); Nigeria needs Paystack because Stripe will not onboard it; FX must be server-side, and fixed local prices read better than live conversion. Note this also settles the backend's open "full actionable audit vs summary" question by making the answer *both, one of them paid* — and that it is now the third way to pay alongside the $149 unlock and the subscription, which need reconciling.

- [ ] **Languages other than English** — proposal in `docs/LANGUAGES.md`. The app has **no translation layer at all**: 181 pieces of wording across 38 files, written directly into the screens. That is the cheap half. The expensive half is that the audit verdict, the action plan, the mentor's answers and both emails are **generated in English by the backend**, which has no language field on the user and no language handling in its prompts — so a picker on its own would give someone a translated interface wrapped around an English audit. Arabic (UAE) mirrors the entire layout and should be its own piece of work; Chinese needs a CJK font before Geist silently falls back. Money and dates already need nothing, being stored in minor units against an ISO currency.

- [ ] **Corporate SSO** — `lookupEmailDomain` resolves entirely on the device and always returns `sso: null`; there is no per-domain IdP directory server-side and **no backend task for one**. The sign-in screen's hand-off path is currently unreachable. Either scope it or remove the affordance — affects F1.3.
- [ ] **Company-registration verification has no backend owner** — affects F1.10.
- [ ] **Push notifications: in or out?** — affects F5.1 and needs a backend task if in.
- [ ] **Does the founder see the full audit or a summary?** — the backend's own open question, and it decides what F2.3 renders — affects F2.3, F4.2.
- [ ] **Web is a preview target, not a product** — the app builds for web (`react-native-web`, `output: "static"`) and that is how it is verified, but `SecureStore` falls back to `localStorage` there and no store listing covers it. Decide whether web ships, before F5.5 makes it look decided.
